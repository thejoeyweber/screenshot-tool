/**
 * Screenshot toolbar component
 * Contains annotation tools and controls
 */
'use client'

import { BaseProps } from '@/types'

export function ToolBar({ className }: BaseProps) {
  return (
    <div className={className}>
      {/* Tool buttons will be added here */}
    </div>
  )
} 